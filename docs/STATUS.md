# 状态 · 2026-09-13

阶段：首个可运行开发原型，不是正式 Benchmark 发行。

工作目录 /mnt/data/arena-combat；目标仓库 hugeJan/arena-combat。用户创建的新仓库已通过连接确认可写。旧仓库与用户 Mac 文件均未修改。

## 已实现

固定上游及素材导入、逐文件摘要、显式外部双控制补丁、有界 JSON 语法、优先级与动作序列、真实执行反馈、同 tick 双方观察、比赛记录、原角色/武器/镜头的观战入口、本局回放、CLI 任务/试战/冻结。

首版不执行任意用户脚本。没有新游戏引擎、账号、模型 API、生产 Genex 连接或云排行榜。盾牌与通用主动蹲避没有实现。

## 本轮实测

本开发容器 Linux x64、Node 22.16.0、Rapier 0.19.0、Three.js 0.180.0。

- 上游实际物理回归：6通过，无跳过，涵盖站立、移动停步、武器、格挡对照与终局。
- 新配置/运行时/真实双控制/CLI 冻结：22通过，无跳过。
- TypeScript 和 Vite 生产构建通过。
- 同环境重复短局的姿态与结果一致。没有宣称跨系统逐位复现。
- 原始开发配置会过度贴近，前两场30秒平局仅4/3个接触事件；保留这一失败探索。
- 修改样例的交战距离与攻击时脚步后，同规则完整换位局出现真实伤害终局。最终代码实测摘要以 `docs/evidence/headless.json` 为准；不把本机拷贝样例当独立 AI 设计。
- 本容器浏览器导航因 `ERR_BLOCKED_BY_ADMINISTRATOR` 被阻止；未绕过。另提供独立 CI 正常本机网页测试，结果必须读取实际产物，不能凭脚本存在认定视觉验收通过。

GitHub 最初源码导入在 git push 阶段遇到服务器500，未把失败称为成功；随后改为同一新仓库内的普通 Git 数据 API 导入，远端已核对源码提交。

## 仍需验证/扩展

原游戏画面接入后的实际浏览器体验和素材载入，用户 Mac 的整场实时表现，独立 AI 的公开资料设计闭环，更多对手与长局，接触顺序/双重失能公平性审计。当前不完整支持通用蹲避、盾防、任意代码策略、跨机器确定性、回放导入或不可篡改的竞赛服务器。
