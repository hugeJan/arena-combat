# 状态 · 2026-09-13

阶段：首个可运行开发原型，不是正式 Benchmark 发行。

工作目录 /mnt/data/arena-combat；新仓库 hugeJan/arena-combat。核心已提交 c1845832cfb3c6bb36d250876be259a561443cc5。旧仓库与用户 Mac 文件未修改。

## 已实现

固定上游及素材导入、逐文件摘要、显式外部双控制补丁、有界 JSON 语法、优先级与动作序列、真实执行反馈、同 tick 双方观察、比赛记录、原角色/武器/镜头观战、本局回放、CLI 任务/试战/冻结。

不执行任意用户脚本。没有新游戏引擎、账号、模型 API、生产 Genex 连接或云排行榜。盾牌与通用主动蹲避没有实现。

## 实际检查

Linux 本地 Node 22.16.0、Rapier 0.19.0、Three.js 0.180.0：上游6项物理测试、新平台22项测试全部通过，无跳过；TypeScript/Vite构建通过。两场完整换位开发对局见 docs/evidence/headless.json。

GitHub运行34750203172检查了真实核心提交c184583：Linux测试与构建通过；macOS ARM64 / Node22.23.2同样22+6项通过，完整换位分别模拟13.075/24.3833秒、实际计算墙钟1.4115/2.0374秒。该结果不是用户MacBook网页帧率；见docs/evidence/macos.json。

同环境重复短局的姿态与结果一致，不外推为跨环境确定性。原开发配置曾因贴近而出现30秒平局、少量接触；修改交战距离与攻击时脚步后产生真实终局。开发样例不是独立AI作品。

## 浏览器及重要限制

开发容器导航被ERR_BLOCKED_BY_ADMINISTRATOR阻止，没有绕过。独立CI在自己的正常环境中启动服务并打开浏览器，已经实际走完开赛、暂停、恢复、真实终局与导出；留有截图与原始对局。初次浏览器测试因使用fill给带小数步长的range输入赋值而失败，已改为真实键盘Home/ArrowRight操作；完整重跑尚待结果，不把部分完成标为通过。

发现同配置、同源码摘要的浏览器与Node初始姿态有微小差异，后续胜负不同。原因尚未定位，不能声称跨运行环境可复现。模型比较必须固定同一执行环境，当前不开放正式排名。

CI无GPU浏览器的画面帧率明显低于无画面计算能力，不能把无画面对局加速宣称为60FPS成品。用户Mac实际流畅度、动作观感仍需实测。

## 下一检查点

完整回放/移动页面检查；浏览器与Node初始化差异定位；更多对手和长局；同tick双重失能及接触处理顺序的公平性审计；公开资料驱动的独立AI设计闭环。尚无通用蹲避、盾防、任意代码策略、跨机器确定性、回放导入、可信远程裁判。

已移除一次性写权限导入/发布工作流，仅保留只读的常规CI。原500推送失败与初次浏览器失败保留在Actions记录，不冒充全部首次通过。
