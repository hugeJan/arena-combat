# 状态 · 2026-09-13

阶段：首个可运行开发原型已提交并完成首轮功能验收，不是正式 Benchmark 发行。

独立工作目录 /mnt/data/arena-combat；新仓库 hugeJan/arena-combat。核心实现 c1845832cfb3c6bb36d250876be259a561443cc5；最后通过全部 CI 的代码提交 49175c1b19fe29e0c91b54539aa7cad17b98abc0。随后交付整理仅修改文档/证据，不改变核心或规则。旧仓库与用户 Mac 文件、服务均未修改。

## 已实现

固定上游及实际素材导入、逐文件摘要、显式外部双控制补丁、有界 JSON 语法、优先级与动作序列、真实执行反馈、同 tick 双方观察、比赛记录、原角色/武器/镜头观战、本局回放、CLI 任务/试战/冻结与 Mac 启动脚本。

不执行任意用户脚本。没有新游戏引擎、账号、模型 API、生产 Genex 连接或云排行榜。盾牌与通用主动蹲避没有实现。

## 实际检查

Linux 本地 Node 22.16.0、Rapier 0.19.0、Three.js 0.180.0：上游6项核心物理测试、新平台22项测试全部通过，无跳过；TypeScript/Vite构建通过。两场完整换位开发对局见 docs/evidence/headless.json。

GitHub运行34750203172检查了核心提交c184583：Linux测试与构建通过；macOS ARM64 / Node22.23.2同样22+6项通过，完整换位分别模拟13.075/24.3833秒、计算墙钟1.4115/2.0374秒。该结果不是用户MacBook网页帧率；见docs/evidence/macos.json。

最终常规CI运行34750392390对提交49175c1完成：core(ubuntu-latest)、core(macos-14)、browser三个job全部completed/success。Linux/macOS分别重跑22项平台测试、6项上游物理测试、生产构建和完整换位对局。没有声称跑过所有上游测试。

同环境同进程重复短局的姿态与结果一致，不外推为跨环境确定性。原开发配置曾因贴近而出现30秒平局、少量接触；修改交战距离与攻击时脚步后产生真实终局。开发样例不是独立AI作品。

## 浏览器实际结果

开发容器本机导航被ERR_BLOCKED_BY_ADMINISTRATOR阻止，没有绕过。独立CI在自己的正常环境中启动预览服务，用Playwright 1.55.0 / Chromium140无头浏览器实际完成：

- 页面加载并显示双方原游戏角色和武器；实际点击开赛，读取运行中tick；
- 暂停后tick稳定，恢复后继续推进，直到真实终局；
- 导出完整本局JSON，进入回放，用Home和60次ArrowRight将时间轴定位02.00；
- 1440像素桌面截图和390像素手机布局，无横向溢出；
- 页面异常errors=[]，请求失败failedRequests=[]。这不是对所有HTTP响应状态或素材内容的完整审计。

已读取实际report.json和ui-match.json，并查看对战与手机截图。浏览器本局蓝方获胜、tick2811、模拟23.425秒。报告、截图摘要和环境见docs/evidence/browser.json；原始截图和对局在运行34750392390的browser产物中（保留7天）。

初次浏览器检查失败原因是fill不接受range的小数步长赋值；已改成真实键盘操作并完整重跑通过。不是删除断言或跳过回放检查。

## 尚未通过的重要目标

**跨环境确定性：** 同配置、同源码摘要在浏览器与Node初始姿态有微小差异，后续胜负不同；差异出现在首个参赛动作之前，原因尚未定位。不能声称跨运行环境可复现，不能混合浏览器/Node成绩排名。见docs/evidence/runtime-difference.json。

**实时图形性能：** 无画面计算有明显余量，但无GPU CI浏览器战斗截图显示6FPS、7.41秒积压，回放报告为9FPS。该环境的功能检查通过不等于实时图形性能通过；用户Mac实际帧率、整场流畅度和动作观感仍需测定。

**评测与玩法完整性：** 同tick双重失能及接触处理顺序的公平性审计、更多对手和长局、独立AI仅使用公开资料完成设计与改进的验收尚未完成。尚无通用蹲避、盾防、任意代码策略、回放导入或可信远程裁判。

## 交付维护

已移除一次性写权限导入/发布工作流，仅保留contents:read的常规CI。原500推送失败与初次浏览器失败保留在Actions记录，不冒充全部首次通过。

当前可克隆main，npm ci后npm run dev；端口127.0.0.1:5180。首版功能可以用于本地制作打法和试战，不作为正式跨模型排名发行。
